# Symlink these directories instead of individual files inside them.
# For git submodules and directories managed by dotfiles.
SYMLINK_DIRS="ohmyzsh vim tmux asdf"

# Excludes these files from home directory.
EXCLUDES="bootstrap.sh keyrings *.gpg rcrc"

# Symlink these directories but without dot prefix.
UNDOTTED="bin"

# Other dotfiles directories to be considered.
# For separating private dotfiles with public ones.
DOTFILES_DIRS=".dotfiles .secrets"
